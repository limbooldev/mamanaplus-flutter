name: mamana_plus_chat
description: Chat feature — domain, data, and presentation (BLoC) for MamanaPlus.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter
  mamana_plus_core:
    path: ../core
  mamana_plus_ui:
    path: ../ui

dev_dependencies:
  flutter_lints: ^5.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
