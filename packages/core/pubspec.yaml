name: mamana_plus_core
description: Shared networking, auth session, env, and errors for MamanaPlus mobile.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.8.1

dependencies:

dev_dependencies:
  lints: ^5.0.0
  test: ^1.24.0
